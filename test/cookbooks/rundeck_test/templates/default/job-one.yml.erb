- loglevel: INFO
  sequence:
    keepgoing: false
    strategy: node-first
    commands:
    - exec: <%= @command %>
  description: <%= @description %>
  nodefilters:
    dispatch:
      threadcount: 1
      keepgoing: false
      excludePrecedence: true
      rankOrder: ascending
    filter: name:.*
  options:
    halp:
      description: I dunno
      value: eval()
